#include <bits/stdc++.h>

using namespace std;

int main()

{
    int number, count1, value;
    cin >> number >> count1 >> value;
    int a[number], b[number];
    for(int i = 0; i < number; ++i)
    {
        cin >> a[i];
    }

    for(int i = 0; i < number; ++i)
    {
        cin >> b[i];
    }

}
