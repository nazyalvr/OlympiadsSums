#include <bits/stdc++.h>

#define ll long long

#define vll vector<long long>

#define start ios_base::sync_with_stdio(false); cin.tie(NULL);

#define range int t; cin >> t; while(t--)

using namespace std;

int n, i, a[100], b[100], c[100], p[100];

int main()
{
    start
    int x, y, x1, y1, ans;
    range
    {
        cin >> x >> y >> x1 >> y1;
        if (x == x1 || y == y1) {ans = -2;} else ans = 0;
        cout << ans + abs(x - x1) + abs(y1 - t);
    }

}
